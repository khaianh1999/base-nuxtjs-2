// import _get from 'lodash/get';

export default async ({ route, store, redirect, $repositories }) => {
    // const res = await $repositories.repository.getUserProfile()
    // const classList = await $repositories.repository.getUserClassList()
    // if (res.data) {
    //   store.commit('SET_USER', res.data)
    //   if (classList.data && classList.data.length && route.path === '/') {
    //     redirect('/tutor')
    //   }
    // } else if (route.path !== '/resetPassword') {
    //   redirect('/')
    // }
    // const authenticateUser = _get(store, 'state.authenticate', null);
    // if (authenticateUser) {
    //     redirect('/');
    // }
};
